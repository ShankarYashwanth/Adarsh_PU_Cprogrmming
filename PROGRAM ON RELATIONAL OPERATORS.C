PROGRAM ON RELATIONAL OPERATORS WITH USING IF ELSE CONDITION



#include <stdio.h>

int main() {
  int num1,num2;
 
  printf("Enter the first integer:");
  scanf("%d",&num1);
  printf("Enter the  second integer:");
  scanf("%d",&num2);
if (num1==num2) {
  printf("two numbers are equal");
} else {
  printf("two numbers are not equal");
}
  return 0;
}